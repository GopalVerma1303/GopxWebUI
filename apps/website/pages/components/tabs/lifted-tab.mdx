---
title: "Lifted Tab"
description: "Elegant and interactive tab components crafted with Tailwind CSS and Framer Motion. Easily integrate and customize to elevate your web applications."
image: "https://webui.gopx.dev/assets/components/tabs/lifted-tab.png"
---

import { Steps } from 'nextra/components'
import { Tabs } from "nextra/components";
import { ComponentPreview } from "@/components/component-preview";
import { ComponentSource } from "@/components/component-source";

# Lifted Tab

<p className="component-description">Elegant and interactive tab components crafted with Tailwind CSS and Framer Motion. Easily integrate and customize to elevate your web applications.</p>

`Tabs` `Animated` `framer-motion`

<ComponentPreview name="lifted-tab-demo" />

## Installation

<Tabs items={["CLI", "Manual"]}>
  <Tabs.Tab>
  ### Run the following command:

```bash
npx gopx-webui add lifted-tab
```
  </Tabs.Tab>
  <Tabs.Tab>
<Steps>
  ### Install `framer-motion`

  ```bash
  npm install framer-motion
  ```

  ### Create `lifted-tab.tsx` component

  <ComponentSource name="lifted-tab" />
</Steps>

  </Tabs.Tab>
</Tabs>

# Props

| Prop          | Type                                    | Required | Description                                                        |
|---------------|-----------------------------------------|----------|--------------------------------------------------------------------|
| `tabs`        | `Array<{ id: string; label: string; }>` | Yes      | An array of tab objects, each containing an `id` and `label`.     |
| `activeTab`   | `string`                                | Yes      | The `id` of the currently active tab.                              |
| `setActiveTab`| `(tabId: string) => void`              | Yes      | A function to update the active tab by setting its `id`.          |

