---
title: "Sticker Components"
description: "Beautifully designed Sticker Components with Tailwind CSS and Framer Motion. Copy, paste, and customize to enhance your web projects effortlessly."
---

import { getPagesUnderRoute } from 'nextra/context'
import Sticker from 'next/link'
import {Cards, Card} from "nextra/components"
import Image from 'next/image'

# Sticker Components

<p className="component-description">Beautifully designed Sticker Components with Tailwind CSS and Framer Motion. Copy, paste, and customize to enhance your web projects effortlessly.</p>

<br />

export function BlogIndex() {
  const currentPageTitle = "Sticker Components";
  return getPagesUnderRoute('/components/stickers')
    .filter(page => page.meta?.title !== currentPageTitle && page.frontMatter?.title !== currentPageTitle)
    .map(page => {
      const fullUrl = page.frontMatter?.image;
      const srcPath = fullUrl?.replace(/^https?:\/\/[^/]+/, '');
      return (
        <ShowcaseCard title={page.meta?.title || page.frontMatter?.title || page.name} href={page.route}>
          <Image src={srcPath} alt={page.frontMatter?.title} width={500} height={300} />
        </ShowcaseCard>
      );
    });
}

export const ShowcaseCard = Object.assign(
  Cards.Card.bind(),
  {
    defaultProps: {
      image: true,
      arrow: true,
    },
    displayName: 'ShowcaseCard',
  }
)

<Cards>
<BlogIndex />
</Cards>
